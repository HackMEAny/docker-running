# docker-running

I need a frontend which will use react & vite. It should show all the running docker containers in that system along with their ports when I click on that it should redirect to a new tab with that running container port like "http://ani.local:9001/" . This entire thing should run in a docker container. Provide a docker compose to run in different system. 